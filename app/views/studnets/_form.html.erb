<%= form_for(@studnet) do |f| %>
  <% if @studnet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@studnet.errors.count, "error") %> prohibited this studnet from being saved:</h2>

      <ul>
      <% @studnet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label : %><br />
    <%= f.text_field : %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
